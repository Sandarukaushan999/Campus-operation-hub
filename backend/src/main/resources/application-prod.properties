# Prod profile.
#
# MongoDB URI and JWT secret MUST come from the environment. We intentionally
# reference env vars with no fallback so startup fails fast if they are unset
# instead of silently running with a weak dev default.
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI}
app.security.jwt-secret=${APP_SECURITY_JWT_SECRET}
