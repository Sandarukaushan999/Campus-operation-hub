# Dev profile. Inherits from application.properties.
# MongoDB URI and JWT secret come from the environment
# (SPRING_DATA_MONGODB_URI / APP_SECURITY_JWT_SECRET)
# and override the defaults from application.properties automatically.
